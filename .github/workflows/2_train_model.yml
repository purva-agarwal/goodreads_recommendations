name: 2. Model Training
run-name: Model Retraining

on:
  workflow_run:
    workflows: ["1. Load Data"]
    types: [completed]

jobs:
  train:
    runs-on: ubuntu-latest
    
    permissions:
      contents: write
      actions: read
    
    if: ${{ github.event.workflow_run.conclusion == 'success' }}

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: 3.11

    - name: Install dependencies
      run: pip install -r model_requirements.txt

    - name: Authenticate to Google Cloud
      uses: google-github-actions/auth@v2
      with:
        credentials_json: ${{ secrets.GCP_CREDENTIALS }}

    - name: Download data artifact via GH CLI
      env:
        GH_TOKEN: ${{ github.token }}
        REPO: ${{ github.repository }}
        RUN_ID: ${{ github.event.workflow_run.id }}
      run: |
        gh run download $RUN_ID -R $REPO -n goodreads-train-data

    - name: Move data to correct location
      run: |
        mv train_data.parquet data/.

    - name: Train the model Bigquery ML (with MLflow)
      env:
        MLFLOW_TRACKING_URI: http://127.0.0.1:5000
      run: |
        echo "Starting MLflow UI in the background..."
        # Start mlflow ui, redirect output to a log file, and run in background (&)
        mlflow ui > mlflow.log 2>&1 &
        
        # Get the Process ID (PID) of the background process
        MLFLOW_PID=$!
        
        # Set a 'trap'. This command will run on EXIT, killing the mlflow process
        # This ensures cleanup even if the python script fails
        trap "echo 'Cleaning up MLflow...'; kill $MLFLOW_PID" EXIT

        echo "Waiting for MLflow server (PID: $MLFLOW_PID) to be ready..."
        
        # Wait up to 30 seconds for the server to respond at the /health endpoint
        timeout 30s bash -c 'until curl -s --fail http://127.0.0.1:5000/health > /dev/null; do echo "Waiting for MLflow..." && sleep 2; done'
        
        echo "MLflow is ready. Running model training..."
        
        # Run your training script
        python -m src.bq_model_training
        
        echo "Training script finished."
        # The 'trap' will automatically run here to kill the MLflow server
    
    - name: Commit and push new files
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      run: |
        # Create a new branch name (e.g., "artifacts_1678886400")
        BRANCH_NAME="artifacts_bot_$(date +%s)"
          
        git config --global user.email "github-actions[bot]@users.noreply.github.com"
        git config --global user.name "github-actions[bot]"
          
        # Switch to the new branch
        git checkout -b $BRANCH_NAME
          
        git add -A
        # Commit the changes
        git commit -m "Add auto generated model artifacts and outputs" || echo "No changes to commit"
          
        # Push the new branch, not master
        git push -u origin $BRANCH_NAME
    
    - name: Notify on success
      if: success()
      run: echo "Model trained successfully"

    - name: Notify on failure
      if: failure()
      run: echo "Model training failed"

  notify:
    needs: train
    if: always()
    uses: ./.github/workflows/send_email.yml
    with:
      status: ${{ needs.train.result }}
      workflow_name: ${{ github.workflow }}
    secrets: inherit
